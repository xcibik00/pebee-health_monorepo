# Supabase — get these from: Supabase Dashboard → Settings → API Keys → "Publishable and secret API keys" tab
# These are PUBLIC values — safe to embed in the app (RLS protects the database)
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_PUBLISHABLE_KEY=your-publishable-key-here

# NestJS backend API — leave unset to auto-detect per platform:
# Android emulator → http://10.0.2.2:3001/api
# iOS simulator    → http://localhost:3001/api
# Override only if your backend runs on a different host/port:
# API_BASE_URL=http://custom-host:3001/api
